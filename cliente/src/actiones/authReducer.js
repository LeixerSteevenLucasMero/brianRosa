// authReducer.js

const initialState = {
    isAuthenticated: false,
    // otras propiedades relacionadas con la autenticación
  };
  
  const authReducer = (state = initialState, action) => {
    // lógica del reducer para manejar acciones relacionadas con la autenticación
    return state;
  };
  
  export default authReducer;
  